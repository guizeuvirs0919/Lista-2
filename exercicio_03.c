#include <stdio.h>

int main() {
    int a, b, c;

    printf("Digite o valor de a: ");
    scanf("%d", &a);

    printf("Digite o valor de b: ");
    scanf("%d", &b);

    printf("Digite o valor de c: ");
    scanf("%d", &c);


    printf("Os valores digitados foram: a = %d, b = %d, c = %d\n", a, b, c);

    return 0;
}
